# Secrets (NEVER commit) ─ Linux port
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/

# Whisper / model caches
models/*.bin
models/*.pt
*.wav
!whisperflow/static/sounds/*.wav

# macOS
.DS_Store
*.swp

# IDE
.idea/
.vscode/
*.sublime-*

# Logs
*.log
/tmp/

# Build
build/
dist/
*.egg-info/

# Config (개인 설정)
# ~/.config/whisperflow/config.json
~/.config/whisperflow/hue_config.json

# Playwright MCP
.playwright-mcp/

# Screenshots (mockup/test) - README 이미지 제외
*.png
# JARVIS UI 스크린샷은 포함 (README용)
!jarvis_*.png
# 앱 아이콘(PWA/파비콘)은 포함
!whisperflow/static/jarvis-icon-*.png

# Windows build
whisperflow_windows.zip
whisperflow_windows/

# Claude Code (로컬 설정 + worktree)
.claude/
.omc/

# YOLO 모델 가중치 (ultralytics 자동 다운로드, 커밋 불필요)
*.pt
